rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Commerce records are server-only. Firebase Admin in Cloud Functions bypasses rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
