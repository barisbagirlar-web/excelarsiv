---
import type { SearchItem } from '../../lib/search';
import HeroSearch from './HeroSearch.astro';

interface Props {
  searchIndex: SearchItem[];
}

const { searchIndex } = Astro.props;
---

<section class="hero-section" aria-labelledby="hero-title">
  <div class="hero-shell">
    <h1 id="hero-title" class="sr-only">Excel Şablonları ve İşletme Çözümleri</h1>
    <picture class="hero-artwork">
      <source media="(max-width: 720px)" srcset="/images/hero-mobile.jpg" />
      <img
        src="/images/hero.jpg"
        alt="Akıllı Kasa Defteri — gerçek Excel şablonunun önizlemesi"
        width="1600"
        height="533"
        fetchpriority="high"
      />
    </picture>
    <div class="hero-search-rail">
      <HeroSearch searchIndex={searchIndex} />
    </div>
  </div>
</section>

<style>
  .hero-section {
    padding-top: 28px;
  }
  .hero-shell {
    position: relative;
    width: min(1480px, calc(100% - 48px));
    margin-inline: auto;
    padding-bottom: 58px;
  }
  .hero-artwork {
    position: relative;
    display: block;
    overflow: hidden;
    aspect-ratio: 3 / 1;
    border-radius: 28px;
    box-shadow: 0 32px 90px rgba(24, 55, 38, 0.16);
  }
  .hero-artwork img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }
  .hero-search-rail {
    position: absolute;
    left: 50%;
    bottom: -34px;
    z-index: 10;
    width: min(820px, calc(100% - 64px));
    transform: translateX(-50%);
  }
  @media (max-width: 720px) {
    .hero-section {
      padding-top: 12px;
    }
    .hero-shell {
      width: calc(100% - 20px);
      padding-bottom: 0;
    }
    .hero-artwork {
      aspect-ratio: 5 / 3;
    }
    .hero-search-rail {
      position: relative;
      left: auto;
      bottom: auto;
      transform: none;
      width: calc(100% - 24px);
      margin: -18px auto 0;
    }
  }
</style>
