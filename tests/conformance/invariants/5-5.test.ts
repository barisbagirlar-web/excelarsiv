import test from'node:test';import assert from'node:assert/strict';import{spawnSync}from'node:child_process';import{resolve}from'node:path';import{fileURLToPath}from'node:url';const ROOT=resolve(fileURLToPath(new URL('../../../',import.meta.url)));test('INV-5.5 privacy review eksik veri varlığı fixture exit 1',()=>{const r=spawnSync(process.execPath,['--experimental-strip-types',resolve(ROOT,'scripts/seo/content-contract.ts'),'--site','excelarsiv','--fixture','privacy-missing'],{cwd:ROOT,encoding:'utf8'});assert.equal(r.status,1);});
