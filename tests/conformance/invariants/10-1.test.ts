import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{resolve}from'node:path';import{fileURLToPath}from'node:url';const ROOT=resolve(fileURLToPath(new URL('../../../',import.meta.url)));function valid(text:string){return['Registry export','Redirect ledger','72 saat','ROLLBACK','KABUL KRİTERİ'].every(x=>text.toLocaleLowerCase('tr-TR').includes(x.toLocaleLowerCase('tr-TR')));}test('INV-10.1 runbook zorunlu ve eksik fixture reddedilir',()=>{const real=readFileSync(resolve(ROOT,'docs/seo/runbooks/MIGRATION_RUNBOOK.md'),'utf8');assert.equal(valid(real),true);assert.equal(valid('# eksik runbook'),false);});
