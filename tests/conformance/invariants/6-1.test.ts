import test from'node:test';import assert from'node:assert/strict';import{spawnSync}from'node:child_process';import{resolve}from'node:path';import{fileURLToPath}from'node:url';const ROOT=resolve(fileURLToPath(new URL('../../../',import.meta.url)));function run(f:string){return spawnSync(process.execPath,['--experimental-strip-types',resolve(ROOT,'scripts/seo/schema-contract.ts'),'--site','excelarsiv','--fixture',f],{cwd:ROOT,encoding:'utf8'});}test('INV-6.1 görünür claim ve entity graph',()=>{assert.equal(run('none').status,0);assert.equal(run('invisible-rating').status,1);});