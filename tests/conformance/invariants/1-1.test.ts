import test from 'node:test';import assert from 'node:assert/strict';import {spawnSync} from 'node:child_process';import {resolve} from 'node:path';import {fileURLToPath} from 'node:url';const ROOT=resolve(fileURLToPath(new URL('../../../',import.meta.url)));function run(f:string){return spawnSync(process.execPath,['--experimental-strip-types',resolve(ROOT,'scripts/seo/registry-validate.ts'),'--site','excelarsiv','--fixture',f],{cwd:ROOT,encoding:'utf8'});}test('INV-1.1 registry canlı set ve duplicate negatif fixture',()=>{assert.equal(run('none').status,0);assert.equal(run('duplicate').status,1);});
