import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{resolve}from'node:path';import{fileURLToPath}from'node:url';const ROOT=resolve(fileURLToPath(new URL('../../../',import.meta.url)));const banned=[/paid link/i,/\bPBN\b/i,/koşulsuz disavow/i,/onaysız HSTS preload/i,/sahte lastmod/i,/cloaking/i];function violations(text:string){return banned.filter(r=>r.test(text)).length;}test('INV-10.2 yasak taraması fixture yakalar ve politika kaydı vardır',()=>{const policy=readFileSync(resolve(ROOT,'docs/seo/runbooks/FORBIDDEN_ACTIONS.md'),'utf8');assert.ok(policy.includes('paid link'));assert.ok(violations('uygulama: paid link ve PBN')>=2);assert.equal(violations('canonical kontrolü ve build testi'),0);});
